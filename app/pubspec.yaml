name: iwasthere_app
description: "HotInsert AI - Put yourself in the hottest moments."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.0

  # Networking
  dio: ^5.7.0

  # Auth
  google_sign_in: ^6.2.0

  # Storage
  flutter_secure_storage: ^9.2.0

  # Video
  video_player: ^2.9.0

  # Image picking
  image_picker: ^1.1.0

  # UI
  cached_network_image: ^3.4.0
  shimmer: ^3.0.0

  # Utilities
  intl: ^0.19.0
  url_launcher: ^6.3.0
  share_plus: ^10.1.0
  path_provider: ^2.1.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
